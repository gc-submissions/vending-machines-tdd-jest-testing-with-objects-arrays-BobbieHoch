function calculateChange(total, payment) {
    let change = payment - total;
    return change;
}

function isSufficientPayment(total, payment) => payment >= total; { };
{
    
}






function calculateTotal() {}
function addItem() {}
function removeItem() {}

module.exports = {
    calculateChange,
    isSufficientPayment,
    calculateTotal,
    addItem,
    removeItem,
};
